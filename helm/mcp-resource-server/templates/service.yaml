apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-resource-server.fullname" . }}
  labels:
    {{- include "mcp-resource-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mcp-resource-server.selectorLabels" . | nindent 4 }}
