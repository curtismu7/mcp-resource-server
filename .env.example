# mcp-resource-server — environment variables
# Copy to .env and fill in real values. This server needs no other service
# to run: every vertical is backed by its own bundled SQLite database.

PORT=8081
HOST=0.0.0.0

# --- Which vertical to serve ---
# A folder under verticals/. Ships with "banking" (default) and "healthcare".
VERTICAL=banking

# --- Resource identity ---
# The audience (aud) this server accepts on inbound bearer tokens. Must match
# the resource/audience you configure on your own PingOne environment for
# whatever OAuth client mints tokens for your MCP caller.
MCP_RESOURCE_SERVER_RESOURCE_URI=your-resource-uri
MCP_SERVER_RESOURCE_NAME=Banking MCP Resource Server

# --- Your PingOne environment (bring-your-own) ---
# Used to build the RFC 9728 discovery metadata at
# /.well-known/oauth-protected-resource.
PINGONE_ENVIRONMENT_ID=your-pingone-env-id
PINGONE_REGION=com

# Token *signature* verification (JWKS) is separate from the two vars above.
# Uncomment ONE of these once you have real PingOne tokens: every inbound
# token is then verified against your environment's keys and rejected on
# failure, regardless of STRICT_AUTH. Leave all three commented out to test
# with hand-made tokens first (see README "Auth modes").
# PINGONE_ISSUER=https://auth.pingone.com/your-pingone-env-id/as
# PINGONE_JWKS_URI=https://auth.pingone.com/your-pingone-env-id/as/jwks
# PINGONE_BASE_URL=https://auth.pingone.com/your-pingone-env-id/as

# Only consulted when NO JWKS source above is set. false (default) accepts a
# well-formed token with a console warning so this runs out of the box;
# true rejects every token instead. Has no effect once a JWKS source is set.
STRICT_AUTH=false
